# Rust
server/target/
server/migrations/.diesel_lock

# Node (apps/web has its own .gitignore)
node_modules/

# Local Claude Code permissions (machine-specific)
.claude/settings.local.json

# graphify knowledge graph (generated locally)
graphify-out/

*.log
